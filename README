slowbot v6.0

now worse than ever

Installation
------------

1) git clone or download source
2) use linux (must have php pcntl module)
3) copy config.php.DIST to config.php
4) edit config.php
5) run like ./bot.php or php -q bot.php
